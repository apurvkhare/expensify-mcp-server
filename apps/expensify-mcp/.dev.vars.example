# Copy to .dev.vars for local overrides (wrangler dev reads it)
API_BASE_URL=http://localhost:8787
PUBLIC_URL=http://localhost:8788
# Owner login password. In production: wrangler secret put OWNER_PASSWORD
OWNER_PASSWORD=change-me
